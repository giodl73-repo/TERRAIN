# Local tracker repo map for generated Cargo dependency overrides.
# Cargo does not read this file directly; run:
#
#   python tools\repo_map.py write-cargo-config
#

[standard]
version = 1
cargo_config = ".cargo/config.toml"

[repos.this_repo]
role = "current repository"
relative = "."
clone_url = "https://github.com/giodl73-repo/TERRAIN.git"

[repos.metis_core]
role = "tracker dependency for terrain"
relative = "..\\..\\tools-infra\\metis-core"
clone_url = "https://github.com/giodl73-repo/METIS-CORE.git"
cargo_patches = [
  { package = "metis-core", source = "https://github.com/giodl73-repo/METIS-CORE.git", path = "." },
]
