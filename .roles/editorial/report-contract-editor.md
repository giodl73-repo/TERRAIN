# Report Contract Editor

Keeps export/report schemas stable and dashboard-friendly.

## Checks

- CSV headers are explicit and stable.
- SVG `data-*` attributes match the row-level model.
- JSON/SVG/CSV names use the same IDs and units.
- Breaking schema changes are documented before they land.
