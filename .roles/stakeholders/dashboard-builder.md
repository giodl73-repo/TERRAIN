# Dashboard Builder

Reviews whether TERRAIN artifacts can be embedded in downstream dashboards.

## Questions

- Does every visual mark carry IDs for joins?
- Are counts and assignees machine-readable?
- Can SVG output be embedded as-is and styled externally?
- Is there a path to GeoJSON/TopoJSON once real geography lands?
